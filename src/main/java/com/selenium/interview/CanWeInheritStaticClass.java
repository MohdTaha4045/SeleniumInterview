package com.selenium.interview;

public class CanWeInheritStaticClass extends CanWeOverloadMethodInStaticClass
{
                  //no we cannot inherit Static Class.
	
	/*In Java, the static keyword is used to indicate that a member
	 (field or method) of a class belongs to the class itself rather
	 than to any instance of the class.
	 When you declare a class as static, you are essentially saying
	 that the class does not need to be instantiated in order to use
	 its static members.
	 */
}
